# Delivery record

Published to `aswinckr/Hatch`, branch `main`, on 8 September 2026. Artifact commit: `0146891b716402a5bc0840d2470a28c2c09a9e77`. GitHub blob hashes for both editable decks, PDF, participant ZIP, instructor package, all three skills, HTML template, and teacher guide matched the prepared files after upload.

## Local work

The clean publishing checkout is `.worktrees/workshop-delivery` inside Hatch on branch `codex/workshop-delivery`. The main local checkout retains its previous HEAD and staged index. Use the publishing checkout for workshop updates; reconcile the existing local main changes deliberately before pulling into that checkout. No reset, force push, or history rewrite was used.

The original local `Design QA Exercise` remains available as the completed instructor application. The GitHub folder of that name now distributes the participant app, and reproducible participant source is under `Workshop/exercise/participant-source`. The finished original/practice/comparison source is also in the sealed instructor package.

## Backup

The verified original project and deck backup is at `/Users/aswin/Documents/1-Projects/Hatch Backups/2026-09-08_21-41-06_before-workshop-preparation`. It contains all prior project files and Git state, original deck exports, the editable Figma backup, and the local instructor-pack key. The key was not uploaded.

## Remaining presenter choices

Fill the two personal TODO slides, choose the model used by attendees, rehearse the account-specific tool path, and arrange participant access. The repository retains its existing private visibility. These are event preparation choices; the deck, handouts, reference skills, app packages, and evaluation records are delivered.
